<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <!-- Background color -->
    <solid android:color="#FF7601" />  <!-- Change color as needed -->

    <!-- Rounded corners -->
    <corners android:radius="150dp" />

    <!-- Optional border -->
    <stroke
        android:width="2dp"
        android:color="#E64A19" />

    <!-- Optional padding inside the shape -->
    <padding
        android:left="10dp"
        android:top="10dp"
        android:right="10dp"
        android:bottom="10dp" />
</shape>
